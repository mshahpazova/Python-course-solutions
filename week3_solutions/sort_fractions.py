# sort_fractions.py
def is_less_than(fraction1, fraction2):
    if fraction1[0] * fraction2[1] < fraction1[1] * fraction2[0]:
        return True
    else
        return False

def sort_fractions():
  pass
